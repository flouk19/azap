import { Meteor } from 'meteor/meteor';
import '../imports/api/votes.js';
import '../imports/api/voteresponses.js';
import '../imports/api/restaurants.js';

Meteor.startup(() => {
  // code to run on server at startup
});
